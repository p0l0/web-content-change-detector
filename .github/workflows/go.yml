name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15

#     - name: Build
#       run: go build -v ./...

    - name: Test
      run: |
        mkdir -p /home/runner/go/src/github.com/p0l0
        ln -s $PWD /home/runner/go/src/github.com/p0l0/web-content-change-detector
        cd /home/runner/go/src/github.com/p0l0/web-content-change-detector
        make test
